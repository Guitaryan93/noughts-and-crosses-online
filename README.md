# Noughts & Crosses Online
Play Noughts & Crosses (tic-tac-toe) online against other people.

Built with Flask, SQLite3, and duct tape...

## Setup
```bash
python -m venv venv        # Setup Python virtual environment
source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
flask run
```
